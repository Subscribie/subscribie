# If a Subscribie site needs to be bootstrapped,
# then these env's need to be set (either manually
# when running locally, or injected using kubernetes
# from within a deployment, for example.
export SUBSCRIBIE_FETCH_JAMLA=True
export BOUCH_DB_SERVICE_NAME=couchdb
export COUCHDB_USER=admin
export COUCHDB_PASSWORD=password
